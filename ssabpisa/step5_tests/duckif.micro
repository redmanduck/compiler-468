PROGRAM duckif
BEGIN

	STRING firstcond := "1stcond\n ";
	STRING scond := "2ndcond\n ";
	INT i,a,num;

	FUNCTION VOID main()
	BEGIN
		num := 7;
		i := 2;	
		a := 42;
	
		IF (i<5)
			WRITE(firstcond);
			num := num +a;
		ELSE
			WRITE(scond);
			num := num - a;
		ENDIF

		WRITE(num);
	END
END
