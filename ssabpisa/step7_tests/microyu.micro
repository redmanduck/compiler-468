PROGRAM fibonacci2
BEGIN

	STRING input := "Please input an integer number: ";
	STRING space := " ";
	STRING eol := "\n";


	FUNCTION INT F (INT n)
	BEGIN
		WRITE(n);
		RETURN 1;
	END
	
	
	FUNCTION INT main ()
	BEGIN
		INT i, end, result;
		i := 0;
		WRITE(input);
		READ(end);

 		WHILE (i != end)
 			WRITE (i);
			result := F(i);
			WRITE (eol);
			i := i + 1;
		ENDWHILE

		RETURN 0;
	END

END	
