PROGRAM microfib
BEGIN
	STRING screw := "duck you: ";
	FUNCTION INT F (INT n)
	BEGIN
		IF (n = 0) 
			RETURN 0;
		ENDIF
		IF (n = 1)
			RETURN 1;
		ENDIF
		RETURN F(n-1) + F(n-2);
	END
	
	
	FUNCTION INT main ()
	BEGIN
		INT i, end, result;
		WRITE(screw);
		READ(i);

		result := F(i);
		WRITE(result);


		RETURN 0;
	END

END	
