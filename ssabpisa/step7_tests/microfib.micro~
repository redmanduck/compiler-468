PROGRAM microfib
BEGIN

	FUNCTION INT F (INT n)
	BEGIN
		IF (n = 0)
		    RETURN 1;
		ENDIF
		RETURN F(n-1) + n;
	END
	
	
	FUNCTION INT main ()
	BEGIN
		INT i, end, result;

		READ(i);

		result := F(i);
		WRITE(result);


		RETURN 0;
	END

END	
